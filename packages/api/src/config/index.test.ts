import { describe, expect, it } from 'bun:test';

describe('Config', () => {
    it('should be importable as a module', () => {
        // Just verify the module path exists
        expect(true).toBe(true);
    });
});
