import Box from '@mui/material/Box';

import UserInfo from '../../components/UserInfo';

const Home = () => {
    return (
        <Box>
            <UserInfo />
        </Box>
    );
};

export default Home;
